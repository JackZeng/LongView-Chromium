# LongView source prototypes

This directory is reserved for LongView-specific prototypes and standalone source that has not yet been placed into an upstream Chromium subtree.

Once a component clearly belongs in Blink, `cc`, `content`, or `chrome`, prefer maintaining the downstream change in a way that mirrors its true Chromium destination rather than building a permanent parallel browser engine here.
