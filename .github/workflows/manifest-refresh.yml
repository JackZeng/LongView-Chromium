name: manifest-refresh-placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reserved excluded path for manifest maintenance."
