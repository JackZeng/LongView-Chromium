name: Retired native source promoter

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Native source promotion is complete; this workflow is retained temporarily for a clean handoff."
