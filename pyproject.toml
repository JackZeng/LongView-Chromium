[project]
name = "longview-chromium-tools"
version = "0.2.0"
description = "Bootstrap, build, run, measure, and probe LongView Chromium"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tools/tests"]
