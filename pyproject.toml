[project]
name = "longview-chromium-tools"
version = "0.1.0"
description = "Bootstrap, build, run, package, and benchmark LongView Chromium"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tools/tests"]
